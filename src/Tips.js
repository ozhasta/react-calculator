export default function Tips() {
  return (
    <div className="tips">
      <p className="tip">Supports touch, click and keyboard.</p>
      <p className="tip">Delete, Esc, Enter, Backspace etc.</p>
    </div>
  )
}
